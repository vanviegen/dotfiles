if test -z "$IN_FISH" -a -n "$PS1" -a -z "$ANTIGRAVITY_AGENT"; then
    export IN_FISH=true
    FISH="$(which fish)"
    if [ "$FISH" = "" ] ; then
        echo fish cannot be found in path: $PATH
    else
        SHELL="$FISH" exec "$FISH"
    fi
fi
