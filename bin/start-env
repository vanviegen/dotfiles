#!/usr/bin/env bash

SUING="`dirname $0`"/../..
export SUING="`realpath $SUING`"

export XDG_CONFIG_HOME="$SUING/.config"

export SHELL=$(which fish)

if [ "$#" -eq 0 ] ; then
  exec "$SHELL" -i
else
  cmd=$(printf "%q " "$@")
  exec "$SHELL" -i -c "$cmd"
fi
